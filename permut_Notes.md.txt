/* 
MIDI API javascript https://github.com/grimmdude/MidiWriterJS#readme 


https://docs.google.com/spreadsheets/d/17DYcHH70APYaHDIF2Zm4CLnPhKjgEmcL8a5LkepBlCs/edit#gid=0

*/ EFFORT TO LOAD BUFFER IN TONE
   // var kickBuff = new Tone.Buffer("sounds/kick.mp3", function () {
    //   //the buffer is now available.
    //   var kick_tone = new Tone.Player(kickBuff.get()).toMaster();
    // });
  
    // var snare_accBuff = new Tone.Buffer("sounds/snare_acc.mp3", function () {
    //   //the buffer is now available.
    //   let snare_acc_tone = new Tone.Player(snare_accBuff.get()).toMaster();
    // });
  
    // var snareBuff = new Tone.Buffer("sounds/snare.mp3", function () {
    //   //the buffer is now available.
    //   let snare_tone = new Tone.Player(snareBuff.get()).toMaster();
    // });
  
    // var hhBuff = new Tone.Buffer("sounds/hats_closed.mp3", function () {
    //   //the buffer is now available.
    //   let hh_tone = new Tone.Player(hhBuff.get()).toMaster();
    // });

  // Tone.Buffer.on('load', function() {
  //   // play instrument sound
  //   let kick_tone = new Tone.Player(kickBuff).toMaster();
  //   let snare_acc_tone = new Tone.Player(snare_accBuff).toMaster();
  //   let snare_tone = new Tone.Player(snareBuff).toMaster();
  //   let hh_tone = new Tone.Player(hhBuff).toMaster();
  //   });